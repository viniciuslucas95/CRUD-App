import styled from 'styled-components';

export const RegisterUserStyle = styled.div`
  display: block;
  margin: 1rem auto;
  width: fit-content;

  input,
  button {
    display: block;
    margin: 0 auto;
  }

  button {
    font-weight: bold;
  }

  input {
    margin: 1rem auto;
  }
`;
