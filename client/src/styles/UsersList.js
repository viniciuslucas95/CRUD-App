import styled from 'styled-components';

export const UsersListStyle = styled.main`
  padding: 1rem 2rem;

  h2 {
    width: fit-content;
    margin: auto;
  }

  div {
    margin: 1rem auto;
    width: fit-content;
  }

  li {
    list-style: none;
    margin-top: 1rem;
    word-break: break-all;
  }
`;
